function setup() {
    var displayWidth = 800;
    var displayHeight = 600;

    var centerX =
        createCanvas(displayWidth, displayHeight);
}

function draw() {    
    background(1, 150, 200);
    textSize(50);
    text('Welcome to AISHA',200,300);
    textSize(20);
    text('Press any key to continue',300,350);

    if (mouseIsPressed === true) {

    }
}
